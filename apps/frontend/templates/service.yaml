apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.name" . }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "frontend.name" . }}
  ports:
    - name: http
      port: 80
      targetPort: 80
